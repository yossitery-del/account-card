/**
 * OG לקישור קצר /j — אותה תמונת תצוגה כמו /join (לזחלנים בלבד).
 * @see src/lib/invitations/inviteExperience.ts
 */
export const runtime = "nodejs";

export {
  default,
  alt,
  size,
  contentType,
} from "../../join/[token]/opengraph-image";
